# 📚 Индекс проекта: Мониторинг конкурентов - AI Ассистент

## 🎯 Общее описание

**Мониторинг конкурентов** — MVP приложение для анализа конкурентной среды с поддержкой мультимодальности (текст и изображения). Приложение использует OpenAI-совместимый API (ProxyAPI) для анализа контента конкурентов.

### Основные возможности:
- ✅ Анализ текста конкурентов (описания, реклама, посты)
- ✅ Анализ изображений (баннеры, скриншоты сайтов, упаковка)
- ✅ Парсинг и анализ веб-сайтов через Selenium Chrome
- ✅ История последних 10 запросов
- ✅ Веб-интерфейс (Vanilla JS)
- ✅ Десктопное приложение (PyQt6)

---

## 📁 Структура проекта

```
pem08-master/
│
├── backend/                    # Backend (FastAPI)
│   ├── __init__.py
│   ├── main.py                 # Главный FastAPI сервер
│   ├── config.py               # Конфигурация и настройки
│   │
│   ├── models/                 # Pydantic модели
│   │   ├── __init__.py
│   │   └── schemas.py          # Схемы запросов/ответов API
│   │
│   └── services/               # Бизнес-логика
│       ├── __init__.py
│       ├── openai_service.py   # Интеграция с ProxyAPI (OpenAI)
│       ├── parser_service.py   # Парсинг веб-страниц (Selenium)
│       └── history_service.py  # Управление историей запросов
│
├── frontend/                   # Веб-интерфейс
│   ├── index.html              # HTML страница
│   ├── styles.css              # CSS стили (тёмная тема)
│   └── app.js                  # JavaScript логика
│
├── desktop/                    # Десктопное приложение (PyQt6)
│   ├── main.py                 # Главное окно приложения
│   ├── api_client.py            # HTTP клиент для backend API
│   ├── styles.py                # Тёмная тема для PyQt6
│   ├── build.py                 # Скрипт сборки .exe
│   ├── CompetitorMonitor.spec  # PyInstaller spec файл
│   ├── requirements.txt         # Зависимости для desktop
│   └── README.md                # Документация desktop приложения
│
├── run.py                      # Скрипт запуска сервера
├── requirements.txt            # Python зависимости (backend)
├── env.example.txt             # Пример переменных окружения
├── history.json                # Файл истории (создаётся автоматически)
├── README.md                    # Основная документация
└── docs.md                      # Документация API

```

---

## 🔧 Технологический стек

### Backend
- **FastAPI** 0.104+ — веб-фреймворк
- **OpenAI** 1.6+ — клиент для ProxyAPI
- **Selenium** 4.15+ — парсинг веб-страниц через Chrome
- **BeautifulSoup4** 4.12+ — парсинг HTML
- **Pydantic** 2.5+ — валидация данных
- **httpx** 0.25+ — HTTP клиент
- **uvicorn** 0.24+ — ASGI сервер

### Frontend
- **Vanilla JavaScript** — без фреймворков
- **CSS3** — тёмная тема с cyan акцентами
- **HTML5** — семантическая разметка

### Desktop
- **PyQt6** — GUI фреймворк
- **PyInstaller** — сборка .exe

### AI/ML
- **ProxyAPI** — OpenAI-совместимый API для России
- **GPT-4o-mini** — модель по умолчанию (текст и vision)

---

## 🏗️ Архитектура

### Backend (FastAPI)

#### `backend/main.py` — Главный сервер
- **Эндпоинты:**
  - `GET /` — главная страница (frontend)
  - `POST /analyze_text` — анализ текста
  - `POST /analyze_image` — анализ изображения
  - `POST /parse_demo` — парсинг и анализ сайта
  - `GET /history` — получение истории
  - `DELETE /history` — очистка истории
  - `GET /health` — проверка работоспособности
  - `GET /docs` — Swagger UI
  - `GET /redoc` — ReDoc документация

- **Middleware:**
  - CORS для работы с frontend
  - Логирование всех HTTP запросов

- **События:**
  - `startup` — инициализация сервисов
  - `shutdown` — закрытие ресурсов (Parser сервис)

#### `backend/config.py` — Конфигурация
- Загрузка переменных окружения из `.env`
- Настройка логирования
- Параметры API (host, port)
- Настройки ProxyAPI (ключ, модели, base URL)
- Параметры парсера (timeout, user-agent)
- Настройки истории (файл, максимум записей)

#### `backend/models/schemas.py` — Модели данных
- **Запросы:**
  - `TextAnalysisRequest` — текст для анализа (мин. 10 символов)
  - `ParseDemoRequest` — URL для парсинга

- **Ответы:**
  - `CompetitorAnalysis` — структурированный анализ (strengths, weaknesses, unique_offers, recommendations, summary)
  - `ImageAnalysis` — анализ изображения (description, marketing_insights, visual_style_score, visual_style_analysis, recommendations)
  - `ParsedContent` — результат парсинга (url, title, h1, first_paragraph, analysis)
  - `TextAnalysisResponse` — ответ на анализ текста
  - `ImageAnalysisResponse` — ответ на анализ изображения
  - `ParseDemoResponse` — ответ на парсинг
  - `HistoryItem` — элемент истории
  - `HistoryResponse` — список истории

#### `backend/services/openai_service.py` — OpenAI сервис
- **Класс:** `OpenAIService`
- **Методы:**
  - `analyze_text(text: str)` — анализ текста конкурента
  - `analyze_image(image_base64: str, mime_type: str)` — анализ изображения
  - `analyze_parsed_content(title, h1, paragraph)` — анализ распарсенного контента
  - `analyze_website_screenshot(screenshot_base64, url, title, h1, first_paragraph)` — комплексный анализ сайта по скриншоту
  - `_parse_json_response(content: str)` — извлечение JSON из ответа модели

- **Особенности:**
  - Использует ProxyAPI (OpenAI-совместимый API)
  - Парсинг JSON из markdown блоков и обычного текста
  - Поддержка vision моделей для анализа изображений
  - Детальное логирование всех операций

#### `backend/services/parser_service.py` — Парсер сервис
- **Класс:** `ParserService`
- **Методы:**
  - `parse_url(url: str)` — асинхронный парсинг URL
  - `_parse_sync(url: str)` — синхронный парсинг (в отдельном потоке)
  - `_create_driver()` — создание Chrome драйвера
  - `screenshot_to_base64(screenshot_bytes: bytes)` — конвертация скриншота в base64
  - `close()` — закрытие executor

- **Особенности:**
  - Использует Selenium с Chrome в headless режиме
  - Автоматическая установка ChromeDriver через webdriver-manager
  - Извлечение: title, h1, первый абзац (мин. 50 символов)
  - Создание скриншота страницы (1920x1080)
  - Ожидание динамического контента (2 сек)
  - Обработка ошибок (timeout, connection errors)

#### `backend/services/history_service.py` — История сервис
- **Класс:** `HistoryService`
- **Методы:**
  - `add_entry(request_type, request_summary, response_summary)` — добавление записи
  - `get_history()` — получение всей истории
  - `clear_history()` — очистка истории
  - `_load_history()` — загрузка из файла
  - `_save_history(history)` — сохранение в файл
  - `_ensure_file_exists()` — создание файла если нет

- **Особенности:**
  - Хранение в JSON файле (`history.json`)
  - Максимум 10 записей (настраивается)
  - Автоматическое удаление старых записей
  - UTF-8 кодировка

### Frontend

#### `frontend/index.html` — HTML структура
- Боковая панель навигации (sidebar)
- 4 вкладки: Анализ текста, Анализ изображений, Парсинг сайта, История
- Зона загрузки изображений (drag & drop)
- Секция результатов анализа
- Overlay загрузки

#### `frontend/app.js` — JavaScript логика
- **Модули:**
  - `state` — состояние приложения
  - `elements` — ссылки на DOM элементы
  - `api` — функции для работы с API
  - `ui` — функции отображения UI
  - `handlers` — обработчики событий

- **Функции:**
  - Переключение вкладок
  - Загрузка и анализ изображений (drag & drop)
  - Отправка запросов к API
  - Отображение результатов анализа
  - Управление историей

#### `frontend/styles.css` — Стили
- Тёмная тема с cyan акцентами
- Адаптивный дизайн
- Градиенты и анимации
- Стили для карточек, кнопок, форм

### Desktop

#### `desktop/main.py` — PyQt6 приложение
- **Классы:**
  - `MainWindow` — главное окно
  - `WorkerThread` — поток для API запросов
  - `DropZone` — зона drag & drop для изображений
  - `ResultBlock` — блок результата анализа

- **Функционал:**
  - 4 вкладки (аналогично веб-версии)
  - Проверка подключения к серверу
  - Индикатор загрузки
  - Отображение результатов анализа
  - Управление историей

#### `desktop/api_client.py` — HTTP клиент
- Синхронные запросы к backend API
- Обработка ошибок
- Проверка здоровья сервера

#### `desktop/styles.py` — Тёмная тема
- CSS стили для PyQt6
- Цветовая схема аналогична веб-версии

---

## 🔌 API Эндпоинты

### Базовый URL
```
http://localhost:8000
```

### 1. Анализ текста
```http
POST /analyze_text
Content-Type: application/json

{
  "text": "Текст конкурента для анализа..."
}
```

**Ответ:**
```json
{
  "success": true,
  "analysis": {
    "strengths": ["..."],
    "weaknesses": ["..."],
    "unique_offers": ["..."],
    "recommendations": ["..."],
    "summary": "..."
  }
}
```

### 2. Анализ изображения
```http
POST /analyze_image
Content-Type: multipart/form-data

file: <изображение>
```

**Ответ:**
```json
{
  "success": true,
  "analysis": {
    "description": "...",
    "marketing_insights": ["..."],
    "visual_style_score": 7,
    "visual_style_analysis": "...",
    "recommendations": ["..."]
  }
}
```

### 3. Парсинг сайта
```http
POST /parse_demo
Content-Type: application/json

{
  "url": "example.com"
}
```

**Ответ:**
```json
{
  "success": true,
  "data": {
    "url": "https://example.com",
    "title": "...",
    "h1": "...",
    "first_paragraph": "...",
    "analysis": { ... }
  }
}
```

### 4. История
```http
GET /history
```

**Ответ:**
```json
{
  "items": [
    {
      "id": "...",
      "timestamp": "2024-01-15T10:30:00",
      "request_type": "text",
      "request_summary": "...",
      "response_summary": "..."
    }
  ],
  "total": 1
}
```

### 5. Очистка истории
```http
DELETE /history
```

### 6. Health check
```http
GET /health
```

---

## ⚙️ Конфигурация

### Переменные окружения (`.env`)

```env
# ProxyAPI (обязательно)
PROXY_API_KEY=your_proxy_api_key_here

# Модели (опционально)
OPENAI_MODEL=gpt-4o-mini
OPENAI_VISION_MODEL=gpt-4o-mini

# API (опционально)
API_HOST=0.0.0.0
API_PORT=8000
```

### Настройки в `backend/config.py`
- `proxy_api_base_url` — базовый URL ProxyAPI
- `history_file` — файл истории (по умолчанию `history.json`)
- `max_history_items` — максимум записей (по умолчанию 10)
- `parser_timeout` — таймаут парсера (по умолчанию 10 сек)
- `parser_user_agent` — User-Agent для парсера

---

## 🚀 Запуск проекта

### 1. Установка зависимостей
```bash
# Backend
pip install -r requirements.txt

# Desktop (опционально)
cd desktop
pip install -r requirements.txt
```

### 2. Настройка переменных окружения
```bash
# Создайте .env файл из env.example.txt
cp env.example.txt .env
# Отредактируйте .env и добавьте PROXY_API_KEY
```

### 3. Запуск сервера
```bash
python run.py
# или
python -m uvicorn backend.main:app --reload --host 0.0.0.0 --port 8000
```

### 4. Доступ к приложению
- Веб-интерфейс: http://localhost:8000
- API документация: http://localhost:8000/docs
- ReDoc: http://localhost:8000/redoc

### 5. Запуск desktop приложения (опционально)
```bash
cd desktop
python main.py
```

---

## 📊 Потоки данных

### Анализ текста
```
Frontend → POST /analyze_text → FastAPI → OpenAIService → ProxyAPI → GPT-4o-mini
                                                                      ↓
Frontend ← JSON Response ← FastAPI ← CompetitorAnalysis ← JSON Parse ←
```

### Анализ изображения
```
Frontend → POST /analyze_image (multipart) → FastAPI → Base64 encode
                                                          ↓
Frontend ← JSON Response ← FastAPI ← ImageAnalysis ← ProxyAPI Vision API
```

### Парсинг сайта
```
Frontend → POST /parse_demo → FastAPI → ParserService → Selenium Chrome
                                                          ↓
                                                      Screenshot + HTML
                                                          ↓
Frontend ← JSON Response ← FastAPI ← CompetitorAnalysis ← Vision API (скриншот + контекст)
```

---

## 🔐 Безопасность

- ✅ API ключи хранятся в `.env` (не в коде)
- ✅ CORS настроен (в продакшене ограничить домены)
- ✅ Валидация входных данных через Pydantic
- ✅ Обработка ошибок и таймаутов
- ⚠️ В продакшене использовать HTTPS
- ⚠️ Добавить `.env` в `.gitignore`

---

## 📝 Логирование

Приложение использует структурированное логирование:
- Уровень: INFO (по умолчанию)
- Формат: `%(asctime)s | %(levelname)-8s | %(name)-25s | %(message)s`
- Логгеры:
  - `competitor_monitor` — основной
  - `competitor_monitor.api` — API запросы
  - `competitor_monitor.openai` — OpenAI сервис
  - `competitor_monitor.parser` — парсер
  - `competitor_monitor.history` — история

---

## 🧪 Тестирование

### Ручное тестирование
1. Запустите сервер: `python run.py`
2. Откройте http://localhost:8000
3. Протестируйте все функции через веб-интерфейс
4. Проверьте API через Swagger UI: http://localhost:8000/docs

### Проверка здоровья
```bash
curl http://localhost:8000/health
```

---

## 📦 Зависимости

### Backend (`requirements.txt`)
- fastapi>=0.104.0
- uvicorn>=0.24.0
- openai>=1.6.0
- httpx>=0.25.0
- python-multipart>=0.0.6
- beautifulsoup4>=4.12.0
- lxml>=5.0.0
- pydantic>=2.5.0
- pydantic-settings>=2.1.0
- python-dotenv>=1.0.0
- aiofiles>=23.2.0
- Pillow>=10.0.0
- selenium>=4.15.0
- webdriver-manager>=4.0.0

### Desktop (`desktop/requirements.txt`)
- PyQt6
- requests (или httpx)

---

## 🎨 UI/UX

### Цветовая схема (тёмная тема)
- Фон: `#0a0f1c` (primary), `#111827` (secondary), `#1a2234` (card)
- Текст: `#f1f5f9` (primary), `#94a3b8` (secondary), `#64748b` (muted)
- Акценты: `#06b6d4` (cyan), `#22d3ee` (cyan light)
- Статусы: `#10b981` (success), `#f59e0b` (warning), `#ef4444` (error)

### Шрифты
- Основной: `Outfit` (Google Fonts)
- Моноширинный: `JetBrains Mono` (Google Fonts)

---

## 🔄 История изменений

Проект находится в стадии MVP. Основной функционал реализован и протестирован.

---

## 📚 Дополнительная документация

- `README.md` — основная документация проекта
- `docs.md` — подробная документация API
- `desktop/README.md` — документация desktop приложения
- `env.example.txt` — пример конфигурации

---

## 🐛 Известные ограничения

1. **Парсер:** Требует установленный Chrome браузер
2. **История:** Хранится локально в JSON файле (не в БД)
3. **Desktop:** Требует запущенный backend сервер
4. **Изображения:** Максимальный размер не ограничен явно (рекомендуется до 10MB)

---

## 🚧 Возможные улучшения

- [ ] Добавить базу данных для истории
- [ ] Реализовать аутентификацию пользователей
- [ ] Добавить экспорт результатов в PDF/Excel
- [ ] Реализовать сравнение нескольких конкурентов
- [ ] Добавить кэширование результатов
- [ ] Улучшить обработку ошибок парсера
- [ ] Добавить поддержку других AI провайдеров
- [ ] Реализовать полностью автономное desktop приложение

---

**Дата создания индекса:** 2024
**Версия проекта:** 1.0.0
